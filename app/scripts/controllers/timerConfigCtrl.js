'use strict';

/**
 * @ngdoc function
 * @name cielotimerApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the cielotimerApp
 */
angular.module('cielotimerApp')
    .controller('timerConfigCtrl', function ($scope, $rootScope, $interval, $window, $sce, dataService) {
    });
